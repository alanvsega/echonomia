import { StyleSheet } from 'react-native';

export default LoaderStyle = StyleSheet.create({
  fullPage: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#1c242c',
  },
});